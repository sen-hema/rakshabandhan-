# Copy the ideas here into Netlify's dashboard, NOT into a real .env file that gets committed.
# Site settings -> Environment variables -> Add a variable

APP_USERNAME=choose-an-id
APP_PASSWORD=choose-a-password
